# Tablets

GameHelper plugin port of the old TabletsAboba tower-tablet highlighter.

It scans visible stash slots by default, detects tower tablets, and draws rarity-colored frames for normal, magic, and rare tablets. Inventory highlighting and optional selected-mod filtering can be enabled from the plugin settings.
